import 'package:flutter/material.dart';

// Placeholder example — replaced in Task 6 with a full demo using
// NativePlaceApiProvider.
void main() {
  runApp(const MaterialApp(
    home: Scaffold(
      body: Center(
        child: Text('google_maps_places_autocomplete_widgets_native example'),
      ),
    ),
  ));
}
